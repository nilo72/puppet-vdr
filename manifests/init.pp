class vdr(
      $config             = $vdr::params::config,
      $package_ensure     = $vdr::params::package_ensure,
      $package_name       = $vdr::params::package_name,
      $service_enable     = $vdr::params::service_enable,
      $service_ensure     = $vdr::params::service_ensure,
      $service_manage     = $vdr::params::service_manage,
      $AudioLanguages     = ['eng', 'deu'],
      $ChannelInfoPos     = 0,
      $ChannelInfoTime    = 3,
      $CurrentChannel     = 12,
      $CurrentDolby       = 0,
      $CurrentVolume      = 125,
      $DefaultLifetime    = 0,
      $DefaultPriority    = 0,
      $DiSEqC             = 0,
      $EPGBugfixLevel     = 0,
      $EPGLanguages       = ['deu','eng'],
      $EPGLinger          = 0,
      $EPGScanTimeout     = 0,
      $InitialChannel     = 0,
      $InitialVolume      = -1,
      $InstantRecordTime  = 555,
      $JumpPlay           = 1,
      $LnbFrequHi         = 10600,
      $LnbFrequLo         = 9750,
      $LnbSLOF            = 11700,
      $LoadMarksInterval  = 2,
      $MarginStart        = 5,
      $MarginStop         = 10,
      $MarkInstantRecord  = 1,
      $MaxVideoFileSize   = 100,
      $MenuButtonCloses   = 0,
      $MenuCmdPosition    = 0,
      $MenuScrollPage     = 1,
      $MenuScrollWrap     = 1,
      $MinEventTimeout    = 300,
      $MinUserInactivity  = 300,
      $MultiSpeedMode     = 0,
      $NameInstantRecord  = 'TITLE EPISODE',
      $OSDHeight          = 486,
      $OSDLanguage        = 1,
      $OSDLeft            = 54,
      $OSDMessageTime     = 1,
      $OSDSkin            = 'Classic_Plugin',
      $OSDTheme           = 'default',
      $OSDTop             = 45,
      $OSDWidth           = 624,
      $PauseLifetime      = 0,
      $PausePriority      = 0,
      $PlayJump           = 1,
      $PrimaryDVB         = 1,
      $PrimaryLimit       = 0,
      $RecordingDirs      = 1,
      $ResumeID           = 0,
      $SetSystemTime      = 0,
      $ShowInfoOnChSwitch = 1,
      $ShowRecDate        = 1,
      $ShowRecLength      = 0,
      $ShowRecTime        = 1,
      $ShowReplayMode     = 0,
      $SplitEditedFiles   = 0,
      $SVDRPTimeout       = 300,
      $TimeoutRequChInfo  = 1,
      $TimeSource         = 0,
      $TimeTransponder    = 0,
      $UpdateChannels     = 4,
      $UseDolbyDigital    = 0,
      $UseSmallFont       = 1,
      $UseSubtitle        = 1,
      $UseVps             = 0,
      $VideoDisplayFormat = 1,
      $VideoFormat        = 0,
      $VpsMargin          = 120,
      $ZapTimeout         = 0
      ) inherits vdr::params{

      class { '::vdr::install': } ~>
      class { '::vdr::config': } ~>
      class { '::vdr::service': }
}
